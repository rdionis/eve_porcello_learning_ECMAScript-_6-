const planet = "Earth";

console.log(planet.startsWith("Ear")); // case sensitive

console.log(planet.endsWith("th")); // case sensitive

console.log(planet.includes("Earth1"));

console.log(planet.includes("art"));

console.log(planet.search("art")); // finds only the first instance
